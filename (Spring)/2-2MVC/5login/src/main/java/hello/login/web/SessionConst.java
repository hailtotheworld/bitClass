package hello.login.web;

public interface SessionConst {
    String LOGIN_MEMBER = "loginMember";
    
    // 상수를 담아 놓고 쓸거다.
    // 일반클래스는 new를 통해 객체가 생성될수가 있으니, 인터페이스 혹은 추상클래스로 만들어서 객체가 생성되지 않도록 한다.
}
